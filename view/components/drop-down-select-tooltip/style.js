import jsxStyle from './../jsx-style/index'

const container = {
    minHeight: '40px',
    padding: '0px 15px'
}

const tooltip = {
    minHeight: '32px',
    maxHeight: '230px',
    minWidth: '120px',
    overflowY: 'auto'
}

const item = {
    height: '32px',
    cursor: 'pointer'
}

const style = {
    container,
    tooltip,
    item
}

export default style