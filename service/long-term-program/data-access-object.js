const dataAccessObject = {
    id: { required: false },

    parentId: { required: true },
    name: { required: true },

    operationalPosition: { required: true }, // for sort
}

export default dataAccessObject
