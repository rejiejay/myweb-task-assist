/**
 * CONST 对外常量与方法:
 * @task task对外常量
 * @utils 常量值对外常用转换方法
 */
import task from './task.js'
import utils from './utils.js'

const defaultPageSize = 15

const CONST = {
    defaultPageSize,
    task,
    utils
}

export default CONST
